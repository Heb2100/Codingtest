#include <iostream>
#include <string>
#include <vector>

using namespace std;

int solution(vector<vector<int>> office, int r, int c, vector<string> move) {
    int answer = 0;

    return answer;
}